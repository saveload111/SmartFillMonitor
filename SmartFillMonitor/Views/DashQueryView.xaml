<UserControl x:Class="SmartFillMonitor.Views.DashQueryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartFillMonitor.Views"
              xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource AppBgDark}" CornerRadius="4" Padding="8" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="开始：" VerticalAlignment="Center" Foreground="White" Margin="0,0,5,0"/>
                <hc:DateTimePicker SelectedDateTime="{Binding StartDate}" Width="120" VerticalAlignment="Center"/>

                <TextBlock Text="结束：" VerticalAlignment="Center" Foreground="White" Margin="10,0,5,0"/>
                <hc:DateTimePicker SelectedDateTime="{Binding EndDate}" Width="120" VerticalAlignment="Center"/>

               
              
                <Button Content="查询" Command="{Binding QueryCommand}" Margin="10,0,0,0" VerticalAlignment="Center" Width="80" Background="#007acc" Foreground="White"/>
                <Button Content="导出CSV" Command="{Binding ExportCommand}" Margin="10,0,0,0" VerticalAlignment="Center" Width="80" Background="{StaticResource AppBgPanel}" Foreground="White"/>

            </StackPanel>

        </Border>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="时间" Binding="{Binding Time,StringFormat=G}" Width="200"/>
                <DataGridTextColumn Header="批次号" Binding="{Binding BatchNo}"/>
                <DataGridTextColumn Header="实产" Binding="{Binding ActualCount }"/>
                <DataGridTextColumn Header="目标" Binding="{Binding TargetCount}"/>
                <DataGridTextColumn Header="设定温度" Binding="{Binding SettingTemp}"/>
                <DataGridTextColumn Header="是否NG" Binding="{Binding IsNG}"/>
            </DataGrid.Columns>
        </DataGrid>
     
    </Grid>
</UserControl>
